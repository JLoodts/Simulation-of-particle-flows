#include "Space.h"

////////////////////////////////////////////////////////////////////
// BEGIN code for Space ///////////////////////////////////////////
Space::Space(InputFile iF)
{
	iF.myFind("-space-");
	iF.get("spacex");	constX	= atof((*iF.location).c_str());
	iF.get("spacey");	constY	= atof((*iF.location).c_str());
	iF.get("spacez");	constZ	= atof((*iF.location).c_str());
}
// END code for Space ///////////////////////////////////////////
////////////////////////////////////////////////////////////////////

